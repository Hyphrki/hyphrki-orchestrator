apiVersion: apps/v1
kind: Deployment
metadata:
  name: agno-runtime
  labels:
    app: agno-runtime
    component: framework-runtime
    framework: agno
spec:
  replicas: 2
  selector:
    matchLabels:
      app: agno-runtime
  template:
    metadata:
      labels:
        app: agno-runtime
        component: framework-runtime
        framework: agno
    spec:
      containers:
      - name: agno-runtime
        image: orchestrator/agno-runtime:latest
        ports:
        - containerPort: 8002
          name: http
        env:
        - name: AGNO_RUNTIME_PORT
          value: "8002"
        - name: AGNO_RUNTIME_HOST
          value: "0.0.0.0"
        - name: OTEL_SERVICE_NAME
          value: "agno-runtime"
        - name: OTEL_TRACES_EXPORTER
          value: "jaeger"
        - name: OTEL_EXPORTER_JAEGER_ENDPOINT
          value: "http://jaeger-collector:14268/api/traces"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: cache
          mountPath: /tmp/cache
      volumes:
      - name: cache
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: agno-runtime
  labels:
    app: agno-runtime
    component: framework-runtime
    framework: agno
spec:
  selector:
    app: agno-runtime
  ports:
  - name: http
    port: 80
    targetPort: http
  type: ClusterIP
